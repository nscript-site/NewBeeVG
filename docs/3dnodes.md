# 3D 节点

## Line3D

3D 线节点，构建方法:

```csharp
NBLine3D Line3D(Vector3 start, Vector3 end, SKColor color, float thickness = 1)
```

## GroundGrid

地表网格，构建方法:

```csharp
NBGroundGrid GroundGrid(int halfLines, float spacing, SKColor color, float thickness = 1)
```
